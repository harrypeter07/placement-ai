{
  "name": "placemint-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["web"],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=web",
    "lint": "npm run lint --workspace=web",
    "worker": "cd telegram-worker && python listener.py",
    "worker:groups": "cd telegram-worker && python list_groups.py"
  }
}
